import 'package:flutter/material.dart';

class AppDarkColors {
  // Brand
  static const primary = Color(0xFF02111B);
  static const accent = Color(0xFF5D737E);

  // Layers
  static const background = Color(0xFF02111B);
  static const surface = Color(0xFF111B24);
  static const elevatedSurface = Color(0xFF1A2630);
  static const modalSurface = Color(0xFF243340);

  // Borders
  static const border = Color(0xFF2F3B45);

  // Text
  static const textPrimary = Color(0xFFFFFFFF);
  static const textSecondary = Color(0xFFB0BEC5);
  static const textMuted = Color(0xFF5D737E);

  // Map
  static const userLocation = Color(0xFF2979FF);
  static const markerDefault = Color(0xFF5D737E);
  static const markerSelected = Color(0xFFFF5252);
  static const radius = Color(0x332979FF);

  // Semantic
  static const success = Color(0xFF00C853);
  static const error = Color(0xFFFF1744);
  static const warning = Color(0xFFFFAB00);
  static const info = Color(0xFF2979FF);
}